# Simple-Note
My simple note

该笔记用于保存较为重要&简短的信息，数据应保存在本地，避免泄露

数据格式:

{
    'shinezone': {
                    'email_name': 'xxx',
                    'email_pwd': 'yyy',
                        xxx
                        xxx
                        xxx
                    }
}


# -*- coding:utf-8 -*-
"""
Django settings for shinezone project.

Generated by 'django-admin startproject' using Django 1.8.

For more information on this file, see
https://docs.djangoproject.com/en/1.8/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/1.8/ref/settings/
"""

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
import os

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DEFAULT_CHARSET = 'utf8'

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/1.8/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'm#$6_!822l77hs-xe20m^dozz^0r4$x5e5znp%snxihjw=zmqh'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = '*'


# Application definition

INSTALLED_APPS = (
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'crm',
    'usp',
    'chart',
    'chart_share'
)
APP_PERMISSION = (
    'crm',
    'chart',
)
MIDDLEWARE_CLASSES = (
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    # 'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.auth.middleware.SessionAuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'django.middleware.security.SecurityMiddleware',
    'middlewares.CRMAuthenticationMiddleware'
)

ROOT_URLCONF = 'urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(os.path.dirname(__file__), '../templates').replace('\\', '/')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                # 'shinezone.crm.utils.template_user'
            ],
        },
    },
]

TEMPLATE_CONTEXT_PROCESSORS = (
    'crm.utils.template_user'
)

WSGI_APPLICATION = 'wsgi.application'


# Database
# https://docs.djangoproject.com/en/1.8/ref/settings/#databases

# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.mysql',     # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
#         'NAME': 'crm',                      # Or path to database file if using sqlite3.
#         # The following settings are not used with sqlite3:
#         'USER': 'root',
#         'PASSWORD': 'root.12359',
#         'HOST': '172.17.1.121',                      # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
#         'PORT': '3306',                           # Set to empty string for default.
#     }
# }

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',     # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.

        # 'NAME': 'crm-for-test',                      # Or path to database file if using sqlite3.
        # 'USER': 'root',
        # 'PASSWORD': 'shinezone244',
        # 'HOST': '54.204.75.32',

        'NAME': 'crm',                      # Or path to database file if using sqlite3.
        'USER': 'root',
        'PASSWORD': 'root.12359',
        'HOST': '172.17.4.244',                      # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'PORT': '3306',                           # Set to empty string for default.
    },
    'chart': {
        'ENGINE': 'django.db.backends.mysql',     # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': 'realtime_etl',                      # Or path to database file if using sqlite3.
        'USER': 'root',
        'PASSWORD': 'root.12359',
        'HOST': '172.17.4.244',                      # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'PORT': '3306',                           # Set to empty string for default.
    },
    'chart_share': {
        'ENGINE': 'django.db.backends.mysql',     # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': 'chart_share',                      # Or path to database file if using sqlite3.
        'USER': 'root',
        'PASSWORD': 'root.12359',
        'HOST': '172.17.4.244',                      # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'PORT': '3306',                           # Set to empty string for default.
    },
}


# Internationalization
# https://docs.djangoproject.com/en/1.8/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True

STATICFILES_DIRS = (
    os.path.join(os.path.dirname(__file__), '../static').replace('\\', '/'),
)

STATIC_URL = '/static/'
STATIC_ROOT = '/data1/www/office.shinezone.com/zone.crm2/shine_static/static'
AUTHENTICATION_BACKENDS = (
    'crm.auth.UserBackend',
)

#CRM_ATTACTMENTS_PATH = os.path.join(os.path.dirname(__file__), '../attachments/crm/').replace('\\', '/')
#UPDATE_PIC_PATH = os.path.join(os.path.dirname(__file__), '../static/img/upload/').replace('\\', '/')
if DEBUG:
    CRM_ATTACTMENTS_PATH = os.path.join(os.path.dirname(__file__), '../static/crm_attachments/').replace('\\', '/')
    UPDATE_PIC_PATH = os.path.join(os.path.dirname(__file__), '../static/img/upload/').replace('\\', '/')
    DOWNLOAd_PATH = os.path.join(os.path.dirname(__file__), '../static/download/').replace('\\', '/')
else:
    CRM_ATTACTMENTS_PATH = STATIC_ROOT + '/crm_attachments/'
    UPDATE_PIC_PATH = STATIC_ROOT + '/img/upload/'
    DOWNLOAd_PATH = STATIC_ROOT + '/download/'
PER_PAGE_NUM = 50

CRM_DOMAIN = 'http://office-test.shinezone.com/'

REPORT_FIGURE_URL = 'http://crm.shinezone.com/index.php?action=%s&imgClass=%s&gId=%s&Sclass=%s'
# REPORT_FIGURE_URL = 'http://192.168.84.129:8000/crm/report/?gid=1&class_pie=%s&imgClass=2&startTime=&endTime=4'

# TODO
PE_TICKET_DETAIL_URL = ''

TEST_EMAIL = '1282594929@qq.com'
# TEST_EMAIL = 'liwenjie@shinezone.com'

SHINEZONE_REDMINE_URL = 'http://redmine.shinezoneapp.com'

if DEBUG:
    GAMES_REDMINE_PROJECT_ID = 'crm-20'
    REDMINE_DEFAULT_USERNAME = 'zhongpengqun'
    REDMINE_DEFAULT_PWD = '261617'
else:
    GAMES_REDMINE_PROJECT_ID = 'onlineqa'
    REDMINE_DEFAULT_USERNAME = 'zhongpengqun'
    REDMINE_DEFAULT_PWD = '261617'


if DEBUG:
    CRM_ATTACTMENTS_PATH = os.path.join(os.path.dirname(__file__), '../static/crm_attachments/').replace('\\', '/')
    UPDATE_PIC_PATH = os.path.join(os.path.dirname(__file__), '../static/img/upload/').replace('\\', '/')
    DOWNLOAd_PATH = os.path.join(os.path.dirname(__file__), '../static/download/').replace('\\', '/')
    REPORT_PIC_PATH = os.path.join(os.path.dirname(__file__), '../static/img/report/').replace('\\', '/')
    SERVER_HOST = 'http://192.168.84.129:8000'
else:
    CRM_ATTACTMENTS_PATH = STATIC_ROOT + '/crm_attachments/'
    UPDATE_PIC_PATH = STATIC_ROOT + '/img/upload/'
    DOWNLOAd_PATH = STATIC_ROOT + '/download/'
    REPORT_PIC_PATH = STATIC_ROOT + '/img/report/'
    SERVER_HOST = 'http://office.shinezone.com'
